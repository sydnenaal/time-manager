language: node_js
node:
    - 12
before_install: npm install lerna
install: npm run boot
script:
    - lerna run --scope 'time-manager-api' build
    - lerna run --scope 'time-manager-frontend' build
