{
    "name": "root",
    "private": true,
    "scripts": {
        "boot": "lerna bootstrap",
        "build": "lerna bootstrap && lerna run --parallel build",
        "start": "node ./api/build/index.js"
    },
    "devDependencies": {
        "lerna": "^3.22.1"
    }
}
